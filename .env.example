# ╔══════════════════════════════════════════════╗
# ║         OpenLoom Configuration                ║
# ║   Generated by install.sh — edit if needed    ║
# ╚══════════════════════════════════════════════╝

# ── Domain (required) ──────────────────────────
# Your domain pointed to this server. Required for HTTPS.
# Example: openloom.example.com
DOMAIN=

# Subdomain for S3 storage. Default: s3.<DOMAIN>
# Must also have a DNS A-record pointed to this server.
S3_DOMAIN=

# ── Security ─────────────────────────────────────
# Secret key for signing JWT tokens.
# MUST be unique. Generate: openssl rand -hex 32
SECRET_KEY=

# ── Public URL ───────────────────────────────────
# Auto-derived from DOMAIN. Override only if using custom proxy.
BASE_URL=https://your-domain.com

# ── Database ─────────────────────────────────────
# Internal PostgreSQL. Don't change unless using external DB.
DATABASE_URL=postgres://openloom:openloom@db:5432/openloom

# ── Storage (S3 / MinIO) ────────────────────────
# Internal MinIO. Don't change unless using external S3.
S3_ENDPOINT=http://minio:9000
S3_BUCKET=openloom
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin

# Public URL for presigned URLs (browser uploads/downloads).
# Auto-derived: https://s3.<DOMAIN>
S3_PUBLIC_URL=https://s3.your-domain.com

# ── Redis ────────────────────────────────────────
# Internal Redis. Don't change unless using external Redis.
REDIS_URL=redis://redis:6379

# ── AI Transcription (Whisper) ──────────────────
# Model: tiny, base, small, medium, large
# Larger = better quality, slower build, more RAM
WHISPER_MODEL=base

# Set to "true" to skip Whisper (faster first build)
DISABLE_TRANSCRIPTION=false

# Set to "false" to skip Whisper compilation (saves RAM during build)
# Auto-disabled by install.sh on servers with <2GB RAM
ENABLE_WHISPER=true
